apiVersion: v1
kind: Pod
metadata:
  name: bad-04-latest
  namespace: policy-test
spec:
  securityContext:
    runAsNonRoot: true
  containers:
    - name: c
      image: nginx:latest
      securityContext:
        privileged: false
      resources:
        requests: { cpu: "50m", memory: "64Mi" }
        limits:   { cpu: "100m", memory: "128Mi" }
